import React from 'react';

const SearchBar = () => {
    return(
        <input type = '' placeHolder='Search... [ctrl+k]' className='h-8 w-fit focus:w-5/6 focus:outline-none transition-all duration-300 ease-in-out bg-gray-50 rounded-full px-4 text-sm'/>
    );
}

export default SearchBar;